<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beijixing.system.mapper.SysFileMapper">

    <resultMap id="BaseResultMap" type="com.beijixing.system.entity.SysFile">
        <id column="id" property="id"/>
        <result column="file_key" property="fileKey"/>
        <result column="original_name" property="originalName"/>
        <result column="file_type" property="fileType"/>
        <result column="file_ext" property="fileExt"/>
        <result column="file_size" property="fileSize"/>
        <result column="storage_type" property="storageType"/>
        <result column="access_url" property="accessUrl"/>
        <result column="upload_user_id" property="uploadUserId"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="tag" property="tag"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectByFileKey" resultMap="BaseResultMap">
        SELECT * FROM sys_file
        WHERE file_key = #{fileKey} AND deleted = 0
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM sys_file
        WHERE upload_user_id = #{userId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectByTag" resultMap="BaseResultMap">
        SELECT * FROM sys_file
        WHERE tag = #{tag} AND upload_user_id = #{userId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

</mapper>
