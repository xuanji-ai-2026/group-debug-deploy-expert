<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beijixing.bxuser.mapper.RolePermissionMapper">

    <resultMap id="BaseResultMap" type="com.beijixing.bxuser.entity.RolePermission">
        <id column="id" property="id"/>
        <result column="role_id" property="roleId"/>
        <result column="permission_id" property="permissionId"/>
        <result column="create_time" property="createTime"/>
        <result column="create_by" property="createBy"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectPermissionIdsByRoleId" resultType="java.lang.Long">
        SELECT permission_id FROM sys_role_permission
        WHERE role_id = #{roleId} AND deleted = 0
    </select>

    <insert id="batchInsert">
        INSERT INTO sys_role_permission (role_id, permission_id, create_time, create_by, deleted)
        VALUES
        <foreach collection="permissionIds" item="permissionId" separator=",">
            (#{roleId}, #{permissionId}, NOW(), #{createBy}, 0)
        </foreach>
    </insert>

    <update id="deleteByRoleId">
        UPDATE sys_role_permission
        SET deleted = 1
        WHERE role_id = #{roleId} AND deleted = 0
    </update>

</mapper>
