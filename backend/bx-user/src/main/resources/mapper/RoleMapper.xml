<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beijixing.bxuser.mapper.RoleMapper">

    <resultMap id="BaseResultMap" type="com.beijixing.bxuser.entity.Role">
        <id column="id" property="id"/>
        <result column="role_code" property="roleCode"/>
        <result column="role_name" property="roleName"/>
        <result column="description" property="description"/>
        <result column="role_type" property="roleType"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="built_in" property="builtIn"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectByRoleCode" resultMap="BaseResultMap">
        SELECT * FROM sys_role
        WHERE role_code = #{roleCode} AND deleted = 0
    </select>

    <select id="selectByTenantId" resultMap="BaseResultMap">
        SELECT * FROM sys_role
        WHERE (tenant_id = #{tenantId} OR tenant_id IS NULL) AND deleted = 0
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT r.* FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND (r.tenant_id = #{tenantId} OR r.tenant_id IS NULL)
          AND r.deleted = 0
          AND ur.deleted = 0
        ORDER BY r.sort_order ASC
    </select>

</mapper>
