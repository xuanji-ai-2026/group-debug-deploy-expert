<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beijixing.bxuser.mapper.UserRoleMapper">

    <resultMap id="BaseResultMap" type="com.beijixing.bxuser.entity.UserRole">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="role_id" property="roleId"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="create_time" property="createTime"/>
        <result column="create_by" property="createBy"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectRoleIdsByUserId" resultType="java.lang.Long">
        SELECT role_id FROM sys_user_role
        WHERE user_id = #{userId}
          AND deleted = 0
    </select>

    <select id="selectUserIdsByRoleId" resultType="java.lang.Long">
        SELECT user_id FROM sys_user_role
        WHERE role_id = #{roleId}
          AND deleted = 0
    </select>

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO sys_user_role (user_id, role_id, tenant_id, create_time, create_by, deleted)
        VALUES
        <foreach collection="roleIds" item="roleId" separator=",">
            (#{userId}, #{roleId}, #{tenantId}, NOW(), #{createBy}, 0)
        </foreach>
    </insert>

    <update id="deleteByUserId">
        UPDATE sys_user_role
        SET deleted = 1
        WHERE user_id = #{userId} AND deleted = 0
    </update>

</mapper>
